from math import sqrt

# invoer
a = float(input('Geef x: '))

# berekening
if a < 2:
    if x == 2:
        mes = '{:.2f} ∉ dom(f)'.format(a)
    else:
        mes = ''
elif x < 2:
    mes = 'x ∉ dom(f)'

# uitvoer
print(mes)
